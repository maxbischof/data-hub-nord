import React from 'react';
import HeaderComponent from '../components/Header'

export default {
  title: 'Header',
  component: HeaderComponent,
};

export const Header = (args) => <HeaderComponent />;